---
"@bonnard/mcp-charts": minor
---

Add an optional `newRow` marker on a dashboard tile. Setting it forces that tile to start a fresh grid row instead of wrapping into leftover space in the current row, giving explicit control over row breaks instead of relying on emergent auto-flow.
