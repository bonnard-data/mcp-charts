---
"@bonnard/mcp-charts": minor
---

Render markdown in text tiles. Bold, italic, lists, links, and headers in a text tile's body now render as formatted output instead of literal markdown syntax. Raw HTML in the source is never passed through, so this is safe for LLM-authored or end-user-typed content.
