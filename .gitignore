node_modules
dist
.turbo
coverage
*.log
.DS_Store
.env
.env.local
__visuals__
gallery.html

# internal/ never ships: research, plans, QA tracking, and runbooks that name
# real infrastructure (tunnel domains, secret refs, prospects). docs/ is
# PUBLIC and ships with the repo — litmus test before adding a file to docs/:
# would you paste it into the README?
internal/
