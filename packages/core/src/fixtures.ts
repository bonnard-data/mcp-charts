// Subpath entry: `import { dashboardFixtures } from "@bonnard/mcp-charts/fixtures"`.
// Shared dashboard fixtures + their input/output types, so consumers reproduce the same specs.
export {
  dashboardFixtures,
  buildCellSpec,
  type DashboardFixture,
  type DashboardFixtureInputs,
  type DashboardItemInput,
  type ChartCellInput,
} from "./fixtures/dashboards.js";
